# 📸 MySight

Простой сервис для поиска фотографий по ключевым словам.

## 🎯 Идея проекта

У вас накапливается много фотографий, и вы хотите, чтобы люди могли находить их через поисковые системы по ключевым словам (как в фотостоках). 

**Пример запроса:** "велосипед детское кресло рассвет город"

## 🚀 MVP (Минимально жизнеспособный продукт)

Простая веб-страница для проверки гипотезы:

- ✅ **Массовая загрузка фотографий** - загружайте несколько фотографий одновременно
- ✅ **Автоматическое определение ключевых слов** - AI определяет содержимое фотографий
- ✅ **Ручное добавление ключевых слов** - возможность редактировать автоматически определенные теги
- ✅ **Поиск по ключевым словам** - находите фотографии по запросам
- ✅ **EXIF данные** - автоматическое извлечение даты, времени, GPS координат
- ✅ **Локальное хранение данных** - все данные хранятся в браузере (localStorage)

## 📋 Как использовать

### Базовое использование

1. Откройте `index.html` в браузере
2. Выберите режим автоматического определения ключевых слов:
   - **Hugging Face API** (бесплатно, работает из коробки)
   - **Google Vision API** (требует API ключ, более точный)
   - **Только EXIF** (дата, время, GPS)
   - **Комбинированный** (EXIF + AI)
3. Загрузите фотографии (можно несколько сразу)
4. Ключевые слова определятся автоматически
5. При необходимости отредактируйте ключевые слова
6. Используйте поиск для нахождения фотографий

### Настройка Google Vision API (опционально)

1. Получите API ключ на https://cloud.google.com/vision
2. Создайте файл `config.js` на основе `config.example.js`
3. Добавьте ваш API ключ в `config.js`
4. Добавьте в `index.html` перед `app.js`:
   ```html
   <script src="config.js"></script>
   ```
5. Выберите "Google Vision API" в выпадающем списке

## 🔧 Технологии

- HTML5
- CSS3 (с градиентами и анимациями)
- Vanilla JavaScript
- LocalStorage для хранения данных

## 📝 Примечания

Это MVP для проверки гипотезы. Данные хранятся локально в браузере (localStorage).

## 🤖 Автоматическое определение ключевых слов

Проект поддерживает несколько способов автоматического определения ключевых слов:

1. **Hugging Face API** - бесплатно, работает без настройки
2. **Google Vision API** - более точный, требует API ключ (1000 бесплатных запросов/месяц)
3. **EXIF данные** - извлечение даты, времени, GPS координат из метаданных фотографий
4. **Комбинированный подход** - объединяет EXIF и AI для максимальной точности

Подробнее см. `AUTO_KEYWORDS_OPTIONS.md`

## 🚀 Деплой

Для работы Hugging Face API нужно задеплоить проект на реальный домен (не localhost).

**Быстрый деплой:**
- Cloudflare Pages (рекомендуется) - см. `DEPLOY.md`
- GitHub Pages - см. `DEPLOY.md`
- Netlify - см. `DEPLOY.md`

После деплоя Hugging Face API будет работать без проблем с CORS.

## 🎯 Следующие шаги

После проверки гипотезы можно добавить:
- Бэкенд для хранения фотографий
- SEO-оптимизацию для индексации поисковыми системами
- Загрузку на сервер
- Публичный доступ к фотографиям
- API для интеграций
- Улучшение точности автоматического определения ключевых слов

